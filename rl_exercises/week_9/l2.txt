2.1.a The imagination horizon has a strong effect on performance. A horizon of 5 achieved the highest final return (315.7), while very short (1) or very long (10 and 20) horizons performed worse. Short horizons provide limited planning, whereas long horizons accumulate prediction errors. Therefore, an intermediate imagination horizon offers the best trade-off between planning depth and model accuracy.
2.1.b The balanced regime achieved the best sample efficiency, reaching the highest average return (315.7). The conservative regime used too little model training and imagination, while the aggressive regime relied too heavily on imagined data, reducing performance due to accumulated model errors. Therefore, a balanced amount of model learning and planning provides the best trade-off.python -m rl_exercises.week_9.dyna_ppo agent.use_model=True agent.max_buffer_size=1000 train.total_steps=15000 +train.model_eval_interval=5000 > buffer_1000.txt

2.1.c The smallest buffer size (1000) achieved the best final return (321.6), while larger buffers did not improve performance. The model MSE was very small for all buffer sizes, but the final return decreased for larger buffers such as 5000, 10000, and 50000. This suggests that using too much old data can hurt learning because the model may train on outdated transitions that are less relevant to the current policy.

2.3 As model noise increased, Dyna-PPO performance generally became less stable and often worse. The no-noise model achieved the best final return (315.7), while higher noise levels reduced performance, especially at 0.05 and 0.20. This shows that Dyna-PPO depends strongly on model accuracy: noisy imagined rollouts can mislead the policy and reduce sample efficiency.